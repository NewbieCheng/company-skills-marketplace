# 安全与信任

Skills 会影响 Codex 的行为，脚本和依赖安装命令还可能修改电脑。只安装你信任的发布者和包。

## 本仓库的安全边界

- 公开用户只有读取权限。
- 只有管理员邀请的公司成员具有直接推送权限。
- 按团队决定，不强制 PR 审核，也不限制依赖安装命令。
- 自动校验会检查结构、路径、常见密钥格式和明显占位内容，但不能证明脚本无恶意行为。
- 系统依赖执行前，Codex 必须展示命令并请求一次确认。

## 禁止上传

- 密码、访问令牌、Cookie、验证码和私钥。
- 授权版 Skill 的内容密钥、签名私钥、客户设备请求码、客户激活码和授权台账。
- 客户个人信息、内部业务数据和未授权素材。
- 指向个人电脑或公司内网的硬编码凭据。
- 规避登录、验证码、付费墙或访问控制的代码。

## 授权版 Skill 的保护边界

- GitHub 仓库只保存薄入口和安装逻辑；核心 Markdown 与编译后的运行器通过 GitHub Release 交付。
- Release 包必须由 `catalog.json` 固定 SHA-256，安装器校验不一致时停止。
- 每个 HGL1 激活码绑定一个 HGD1 设备请求码，不能当作多人共用密码。
- 一期目标是防普通查看、随手复制、跨设备转卖和简单篡改，不承诺抵抗管理员内存抓取、模型代理记录或长期专业逆向。
- 一期宿主 AI 会在任务执行时收到解密后的受保护上下文；控制本机 MCP 通信、进程或内存的人可能获取它。
- 一期运行器尚未完成 Windows 商业代码签名和 macOS Developer ID 公证，不能把 SHA-256 校验等同于操作系统发布者签名。
- 完全离线授权无法远程撤销；合同、客户台账和换机规则仍然必需。

## 报告问题

不要在公开 Issue 中粘贴真实密钥或隐私数据。请通过 GitHub 仓库的 Security 页面提交私密漏洞报告；如果该功能不可用，先联系仓库所有者 `NewbieCheng`。
